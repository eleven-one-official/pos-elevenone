# Base URL of the Laravel API. Defaults to the local artisan server when unset.
# For the waiter tablets, point this at the POS machine's LAN address, e.g.
# VITE_API_URL=http://192.168.1.10:8000/api
VITE_API_URL=http://127.0.0.1:8000/api
