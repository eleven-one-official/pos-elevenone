# Base URL of the Laravel API. Defaults to the local artisan server when unset.
# The backend runs on port 8001 (BYD owns 8000). For the waiter tablets, point
# this at the POS machine's Caddy TLS address, e.g.
# VITE_API_URL=https://192.168.1.10:8443/api
VITE_API_URL=http://127.0.0.1:8001/api
